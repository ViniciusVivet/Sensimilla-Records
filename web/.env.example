# URL pública do site (sem barra no final). Usada em metadata, OG, sitemap e robots.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Opcional: endpoint para envio do formulário de contato (ex.: API Route + Resend, ou webhook).
# NEXT_PUBLIC_CONTACT_FORM_ACTION=
